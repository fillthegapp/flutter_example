import 'package:flutter/material.dart';

class MuseumListScreen extends StatelessWidget {
  const MuseumListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Museum list screen"));
  }
}
